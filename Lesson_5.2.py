# import requests
# import inspect
#
# print(inspect.getmodule(requests.get))
# print(inspect.getmodule(list))


import random
import inspect

print(inspect.getmodule(random))
print(inspect.getmodule(list))